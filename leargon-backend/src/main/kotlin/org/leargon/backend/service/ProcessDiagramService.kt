package org.leargon.backend.service

import jakarta.inject.Singleton

/**
 * Kept as an empty placeholder to avoid breaking any test imports.
 * Diagram logic has moved to ProcessFlowService (storage) and BpmnExportService (BPMN export).
 */
@Singleton
open class ProcessDiagramService
