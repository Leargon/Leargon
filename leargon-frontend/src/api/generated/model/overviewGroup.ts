/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Léargon API
 * REST API for Léargon data and process governance platform.
 *
 * ## Authentication
 * The API uses JWT (JSON Web Token) based authentication. After logging in or signing up, you will receive an access token that must be included in the `Authorization` header as a Bearer token for protected endpoints.
 *
 * ## Authorization Roles
 * - **ROLE_USER**: Standard user role, assigned to all users by default
 * - **ROLE_ADMIN**: Administrative role with full user management capabilities
 *
 * ## Fallback Administration Protection
 * The system includes a fallback admin user that cannot be modified or deleted through the API. Any attempt to modify this user will result in a 403 Forbidden response.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { LocalizedText } from './localizedText.ts';
import type { OverviewNode } from './overviewNode.ts';

export interface OverviewGroup {
  /**
     * Identifies the grouping value — a username, an org-unit key, an enum value. Null is the unassigned bucket, for items with no value for this dimension.
     * @nullable
     */
  key?: string | null;
  /**
     * i18n key for the heading, set when the grouping value is a closed enum the UI already translates (`processType.OPERATIONAL_CORE`, `orgUnitType.TEAM`) or the unassigned bucket (`common.unassigned`). The client renders this key and falls back to `labels`. Null when the value is user content, which has no key and arrives translated in `labels`.
     * @nullable
     */
  labelKey?: string | null;
  /** Group heading in every active locale. For a grouping value that is user content (a unit or bounded-context name) these are the record's own translations; for an enum value it is the English fallback behind `labelKey`. */
  labels: LocalizedText[];
  /** Number of items that actually belong to the group; context-only ancestors are not counted */
  itemCount: number;
  /** Roots of the sub-trees in this group */
  nodes: OverviewNode[];
}
