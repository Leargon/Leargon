/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Léargon API
 * REST API for Léargon data and process governance platform.
 *
 * ## Authentication
 * The API uses JWT (JSON Web Token) based authentication. After logging in or signing up, you will receive an access token that must be included in the `Authorization` header as a Bearer token for protected endpoints.
 *
 * ## Authorization Roles
 * - **ROLE_USER**: Standard user role, assigned to all users by default
 * - **ROLE_ADMIN**: Administrative role with full user management capabilities
 *
 * ## Fallback Administration Protection
 * The system includes a fallback admin user that cannot be modified or deleted through the API. Any attempt to modify this user will result in a 403 Forbidden response.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { AdvisorAnswer } from './advisorAnswer.ts';
import type { LocalizedText } from './localizedText.ts';

export interface AdvisorEvaluateRequest {
  ruleSetCode: string;
  answers: AdvisorAnswer[];
  /**
     * The item the user started from (e.g. "Add child" on Order). The server pre-answers the questions that lead to it and every picker asking for an item of its type, so the tree starts with the questions about that item. 400 when the key is not an item of the rule set's start type.
     * @nullable
     */
  contextItemKey?: string | null;
  /**
     * The name the user intends to use, for the duplicate check at the recommended place
     * @nullable
     */
  proposedNames?: LocalizedText[] | null;
}
