/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * Léargon API
 * REST API for Léargon data and process governance platform.
 *
 * ## Authentication
 * The API uses JWT (JSON Web Token) based authentication. After logging in or signing up, you will receive an access token that must be included in the `Authorization` header as a Bearer token for protected endpoints.
 *
 * ## Authorization Roles
 * - **ROLE_USER**: Standard user role, assigned to all users by default
 * - **ROLE_ADMIN**: Administrative role with full user management capabilities
 *
 * ## Fallback Administration Protection
 * The system includes a fallback admin user that cannot be modified or deleted through the API. Any attempt to modify this user will result in a 403 Forbidden response.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { TaskRuleDefinitionDefaultPriority } from './taskRuleDefinitionDefaultPriority.ts';
import type { TaskRuleDefinitionMaturityLevel } from './taskRuleDefinitionMaturityLevel.ts';

export interface TaskRuleDefinition {
  ruleCode: string;
  /** Human-readable rule name (English fallback; the UI prefers its own i18n key) */
  label: string;
  /** What the rule checks and why it matters, in English. Like `label`, this is a fallback: the UI renders `tasks.ruleDescriptions.<ruleCode>` from its own i18n and only falls back to this. */
  description: string;
  /** Entity type the rule applies to (BUSINESS_ENTITY, BUSINESS_DOMAIN, BUSINESS_PROCESS, ORGANISATIONAL_UNIT) */
  entityType: string;
  /** @nullable */
  methodology?: string | null;
  section: string;
  maturityLevel: TaskRuleDefinitionMaturityLevel;
  defaultPriority: TaskRuleDefinitionDefaultPriority;
  /** Whether the rule is active when no configuration has been saved (BASIC tier only) */
  enabledByDefault: boolean;
}
