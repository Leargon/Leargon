/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Léargon API
 * REST API for Léargon data and process governance platform.
 *
 * ## Authentication
 * The API uses JWT (JSON Web Token) based authentication. After logging in or signing up, you will receive an access token that must be included in the `Authorization` header as a Bearer token for protected endpoints.
 *
 * ## Authorization Roles
 * - **ROLE_USER**: Standard user role, assigned to all users by default
 * - **ROLE_ADMIN**: Administrative role with full user management capabilities
 *
 * ## Fallback Administration Protection
 * The system includes a fallback admin user that cannot be modified or deleted through the API. Any attempt to modify this user will result in a 403 Forbidden response.
 *
 * OpenAPI spec version: 1.0.0
 */
import type { LocalizedText } from './localizedText.ts';
import type { TaskItemPriority } from './taskItemPriority.ts';
import type { TaskItemResourceType } from './taskItemResourceType.ts';
import type { TaskItemResponsibility } from './taskItemResponsibility.ts';
import type { TaskItemSeverity } from './taskItemSeverity.ts';

export interface TaskItem {
  /** Deterministic task identifier: <entityType>:<resourceKey>:<ruleCode>:<fieldName> */
  id: string;
  resourceType: TaskItemResourceType;
  /** Key of the catalogue item the task belongs to */
  resourceKey: string;
  /** Localised names of the catalogue item */
  resourceNames: LocalizedText[];
  /** Machine-readable rule code; the frontend renders its label from i18n */
  ruleCode: string;
  /** REQUIRED = should do, RECOMMENDED = could do */
  priority: TaskItemPriority;
  severity: TaskItemSeverity;
  /**
     * Field the task targets, used to deep-link into the detail panel
     * @nullable
     */
  fieldName?: string | null;
  /** Localised, human-readable name of that field (e.g. "Description (en)", "Classification: Data Sensitivity"). Empty when the field name matches nothing known, in which case the caller shows the raw fieldName. */
  fieldLabels?: LocalizedText[];
  /**
     * Methodology section of the targeted field (CORE, GDPR, DDD, …)
     * @nullable
     */
  section?: string | null;
  /**
     * Methodology owning this rule; null for methodology-independent rules
     * @nullable
     */
  methodology?: string | null;
  /** Why this task is on the user's list */
  responsibility: TaskItemResponsibility;
  /** True when the current user has dismissed this task and the item has not changed since */
  dismissed: boolean;
  /** @nullable */
  dismissedReason?: string | null;
}
