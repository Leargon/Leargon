apiVersion: v1
kind: Service
metadata:
  name: {{ include "leargon.fullname" . }}-backend
  labels:
    {{- include "leargon.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leargon.backend.selectorLabels" . | nindent 4 }}
