Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}

To check the status of your deployment:
  kubectl get pods -l "app.kubernetes.io/instance={{ .Release.Name }}"

{{- if .Values.ingress.enabled }}

The application is accessible via ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}

To access the application, run:
  # Backend API
  kubectl port-forward svc/{{ include "leargon.fullname" . }}-backend {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}

  # Frontend
  kubectl port-forward svc/{{ include "leargon.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}
{{- end }}
