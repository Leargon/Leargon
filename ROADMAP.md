# Retention rule
**AS A** data owner or admin\
**I WANT** to define a retention rule for a business entity\
**SO THAT** everyone can check this requirement

# Quality checks rules
**AS A** data owner or admin\
**I WANT** to define quality check rules for a business entity\
**SO THAT** everyone can check this requirement

# Statistics
**AS AN** admin\
**I WANT** to define statistical metrics, which are calculated repeatedly\
**SO THAT** I can check for completeness or compliance

# Optionals
**AS AN** admin\
**I WANT** to define, which fields (also locales) are mandatory\
**SO THAT** I can configure to the needs of the company

# Visualising business entities
**AS A** user\
**I WANT** to see the relationship and implementations like ERD and Class diagram, with depth set by the viewing user\
**SO THAT** I can see, how the entity is used
**AND** also optionally, I can enable the domain layer, where the entities are shown inside of a domain (container) which is assigned

# Visualising business processes
**AS A** user\
**I WANT** to see the BPMN-diagram, where the subprocess can be expanded or collapsed\
**SO THAT** I can see the whole process
**AND** also optionally, I can enable the organisational layer, where the processes are shown inside of a organisational unit (container) which the executional unit is assigned
**AND** also optionally, I can enable the domain layer, where the processes are shown inside of a domain (container) which is assigned

# Visualising business entities overview
**AS A** user\
**I WANT** to see the relationship and implementations like ERD and Class diagram, with depth set by the viewing user\
**SO THAT** I can see the holisitic view on all business entities
**AND** also optionally, I can enable the domain layer, where the entities are shown inside of a domain (container) which is assigned

# Visualising business processes overview
**AS A** user\
**I WANT** to see the BPMN-diagram (including entity input and output), where the subprocess can be expanded or collapsed\
**SO THAT** I can see the holisitic view on all business processes
**AND** also optionally, I can enable the organisational layer, where the processes are shown inside of a organisational unit (container) which the executional unit is assigned
**AND** also optionally, I can enable the domain layer, where the entities are shown inside of a domain (container) which is assigned

# Visualising company
**AS A** user\
**I WANT** to see a combination of entity-process, entity-domain, process-domain, process-orgunit of all company\
**SO THAT** I can reflect on the relationships and dependencies 

# Stewards
**AS AN** admin or owner\
**I WANT** to set zero, one, or many stewards\
**SO THAT** the stewards are also able to edit

# Governance processes
**AS AN** owner\
**I WANT** to confirm or modify the contents on a repeating basis\
**SO THAT** it is audited, that the content is still correct

# Communication
**AS** Léargon
**I WANT** to be able to send e-mails or teams-webhook to notify owners or teams-channel about upcoming governance checkpoints or changes in contents
**SO THAT** the owners are notified on governance checkpoint, or user are notified on watched items